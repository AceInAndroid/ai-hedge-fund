Use [SKILL.md](./SKILL.md) as the canonical repository skill file.

Recommended load order:

1. `SKILL.md`
2. `references/configuration.md`
3. `references/workflows.md`

Execution preference:

- use the bundled `scripts/` entrypoints instead of reconstructing long shell commands from prose
